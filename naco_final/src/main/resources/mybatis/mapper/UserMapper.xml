<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.naco.mapper.UserMapper">

    <select id="findByUsername" parameterType="string" resultType="org.example.naco.domain.User">
        SELECT id, username, password_hash AS passwordHash, role, created_at AS createdAt
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findAll" resultType="org.example.naco.domain.User">
        SELECT id, username, password_hash AS passwordHash, role, created_at AS createdAt
        FROM users
        ORDER BY id DESC
    </select>

    <update id="updateRole">
        UPDATE users
        SET role = #{role}
        WHERE id = #{id}
    </update>

    <insert id="insert" parameterType="org.example.naco.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, role, created_at)
        VALUES (#{username}, #{passwordHash}, #{role}, NOW())
    </insert>

</mapper>
