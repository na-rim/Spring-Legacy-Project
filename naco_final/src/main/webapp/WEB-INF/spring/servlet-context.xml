<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <mvc:annotation-driven/>
    <context:component-scan base-package="org.example.naco.controller"/>

    <!-- Static resources (optional) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    <mvc:resources mapping="/css/**" location="/css/" />

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <mvc:interceptors>
        <!-- Request log interceptor (shows Interceptor lifecycle in action) -->
        <bean class="org.example.naco.interceptor.RequestLogInterceptor"/>

        <!-- Login required URLs -->
        <mvc:interceptor>
            <mvc:mapping path="/tasks/**"/>
            <mvc:mapping path="/board/**"/>
            <mvc:mapping path="/problems/**" />
            <mvc:exclude-mapping path="/"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/register"/>
            <mvc:exclude-mapping path="/logout"/>
            <mvc:exclude-mapping path="/resources/**"/>
            <mvc:exclude-mapping path="/css/**" />
            <bean class="org.example.naco.interceptor.LoginInterceptor"/>
        </mvc:interceptor>

        <!-- Admin only URLs (optional) -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <mvc:exclude-mapping path="/resources/**"/>
            <bean class="org.example.naco.interceptor.AdminInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>
